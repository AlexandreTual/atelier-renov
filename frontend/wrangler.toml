name = "atelier-renov"
pages_build_output_dir = "dist"

# Build command: npm run build
# Set in Cloudflare Pages dashboard (Settings → Builds & deployments):
#   Build command:    npm run build
#   Build output dir: dist
#   Root directory:   frontend
#   Node.js version:  20

# Environment variables to set in Pages dashboard:
#   VITE_API_URL = https://atelier-renov-api.<account>.workers.dev
#   (or your custom domain for the Worker)
